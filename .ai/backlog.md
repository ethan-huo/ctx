# Deferred Work

- YouTube transcript reads currently depend on `yt-dlp` being available on `PATH`. If we want zero external runtime dependencies, we need a native caption-resolution path with the same stability before removing that dependency.
